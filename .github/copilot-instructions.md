Exit code 130 is a bug in VSCode terminal, just retry if you get that.

Use `ocaml.server.restart` command to restart the OCaml language server after performing a `dune build`.

Use `get_errors` command to find highlighted errors in project files.
